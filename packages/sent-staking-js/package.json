{
  "name": "@session/sent-staking-js",
  "version": "0.0.0",
  "private": true,
  "exports": {
    "./*": "./*.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@session/typescript-config": "workspace:*",
    "@session/eslint-config": "workspace:*"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=9",
    "yarn": "use pnpm",
    "npm": "use pnpm"
  }
}
